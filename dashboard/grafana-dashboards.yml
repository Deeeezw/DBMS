apiVersion: 1

providers:
  - name: 'Distributed DB Dashboards'
    orgId: 1
    folder: 'Distributed DB'
    folderUid: 'distributed-db'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
